import styled from 'styled-components'

export const LinkContainer = styled.a`
  height: 100%;
  display: flex;
  align-items: center;
  color: ${(props) => props.theme.blue};
  font-weight: 700;
  font-size: 1.2rem;
`
